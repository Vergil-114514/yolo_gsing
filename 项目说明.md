# 项目说明

## 概述

YOLOv8 Nano 检测模型训练 + OpenVINO INT8 轻量化，目标部署硬件为 Intel N100 小主机（Ubuntu 22.04）。

检测 4 类物资：Cube_food, Cube_ins, Cube_medicine, Cube_tool。

## 技术栈

- Python 3.12
- ultralytics 8.3.217 (YOLOv8 训练)
- OpenVINO 2024.6 (推理 / INT8 量化)
- NNCF (神经网络压缩框架)
- OpenCV (图像处理)

## 目录结构

```
.
├── yolov8n.pt                    # 预训练权重（训练起点）
├── yolo11n.pt                    # YOLO11 预训练权重（备用）
├── train.py                      # 训练脚本
├── split_data.py                 # 数据集划分脚本
├── quantize_int8.py              # OpenVINO INT8 量化脚本
├── validate_and_benchmark.py     # FP32 vs INT8 对比验证脚本
├── inference_n100.py             # N100 部署推理脚本
├── Cube.yolov8/
│   ├── data.yaml                 # 数据集配置（nc=4）
│   ├── data_split.yaml           # 划分后数据集配置（由 split_data.py 生成）
│   ├── train/images/             # 845 张训练图片
│   ├── train/labels/             # 845 个标签文件（polygon 格式）
│   └── split/                    # 划分后 train/valid/test（由 split_data.py 生成）
└── runs/cube_yolov8n_n100/
    ├── baseline/weights/         # 训练产出 best.pt + last.pt
    ├── openvino_fp32/            # OpenVINO FP32 模型（11.8 MB）
    ├── openvino_int8/            # OpenVINO INT8 模型（3.5 MB）— 部署用
    └── benchmark_results.*       # 对比验证结果
```

## 运行方式

### 训练

```bash
python split_data.py   # 划分数据集
python train.py        # 训练 yolov8n，100 epochs
```

### 导出 + 量化

```bash
yolo export model=runs/cube_yolov8n_n100/baseline/weights/best.pt format=openvino
cp runs/.../best_openvino_model/best.* runs/.../openvino_fp32/
python quantize_int8.py
```

### 验证对比

```bash
python validate_and_benchmark.py
```

### N100 推理

```bash
pip install openvino opencv-python numpy
python inference_n100.py -s ./test.jpg
python inference_n100.py -s ./images/ -o ./output/
python inference_n100.py -s ./video.mp4 -o ./result.mp4
```

## 变更日志

| 日期 | 改动 | 原因 |
|------|------|------|
| 2026-05-30 | 数据集清洗：删除占位 Cube 类，5 类重映射为 4 类 | 只处理 Cube 类物资 |
| 2026-05-30 | split_data.py 改为动态计数、每次重建 split | 适配新数据集 845 张图片 |
| 2026-05-30 | 训练 yolov8n，mAP50=0.989 | 重新训练匹配新 4 类数据 |
| 2026-05-30 | OpenVINO FP32 导出 + INT8 量化 | 轻量化适配 N100 |
| 2026-05-30 | validate_and_benchmark.py 修复 class score 索引和 polygon 标签解析 | 原代码 YOLOv8 输出解析和标注读取有 bug |
| 2026-05-30 | 新增 inference_n100.py | N100 部署推理脚本 |
| 2026-05-31 | 代码审查修复（详见变更日志） | 修复硬编码路径、检测格式不一致、InferRequest 复用、mAP 验收逻辑、重复读图等 14 个问题 |
